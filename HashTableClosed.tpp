template <typename T>
int HashTableClosed<T>::insert(const T& key)
{
    // TO DO:
    return 0;
}

template <typename T>
pair<bool, int> HashTableClosed<T>::search(const T& key) const
{
    // TO DO: 
    return {false, 0};
}
